<Window x:Class="SmartX.Desktop.Views.TelemetryWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
Title="Smart-X Telemetry Dashboard"
Height="650"
Width="1100"
WindowStartupLocation="CenterScreen"
Background="#F4F6F8">

    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">

            <TextBlock Text="Sensor Data Ingestion &amp; Telemetry"
FontSize="28"
FontWeight="Bold"/>

            <TextBlock Text="Registered Smart-X IoT devices"
Margin="0,8,0,0"
Foreground="#666666"
FontSize="15"/>

            <Button x:Name="btnRefresh"
Content="Refresh Devices"
Width="150"
Height="35"
HorizontalAlignment="Left"
Margin="0,20,0,0"
Click="btnRefresh_Click"/>

        </StackPanel>

        <DataGrid x:Name="dgDevices"
Grid.Row="2"
AutoGenerateColumns="False"
IsReadOnly="True"
CanUserAddRows="False"
HeadersVisibility="Column"
GridLinesVisibility="Horizontal">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Device ID"
Binding="{Binding DeviceId}"
Width="140"/>

                <DataGridTextColumn Header="MAC Address"
Binding="{Binding MacAddress}"
Width="180"/>

                <DataGridTextColumn Header="Location"
Binding="{Binding DeploymentLocation}"
Width="*"/>

                <DataGridTextColumn Header="Category"
Binding="{Binding Category}"
Width="120"/>

                <DataGridTextColumn Header="Health"
Binding="{Binding HealthStatus}"
Width="100"/>

                <DataGridTextColumn Header="Status Message"
Binding="{Binding HealthMessage}"
Width="250"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</Window>








